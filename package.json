{
  "name": "university-dashboard",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Build handled by nixpacks'",
    "start": "cd backend && gunicorn dashboard_project.wsgi --bind 0.0.0.0:$PORT"
  }
}
